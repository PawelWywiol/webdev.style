{
  "name": "webdev.style",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "rm -rf .next && next build",
    "start": "NODE_ENV=production node app.js",
    "export": "rm -rf .next && next build && next export",
    "clean": "rm -rf .next",
    "restart": "devil www restart webdev.style",
    "deploy": "npm i --only=dev && devil www restart webdev.style"
  },
  "dependencies": {
    "@emotion/react": "^11.7.1",
    "@emotion/styled": "^11.6.0",
    "emotion-normalize": "^11.0.1",
    "next-translate": "^1.3.3",
    "plyr": "^3.6.12",
    "react": "17.0.2",
    "react-device-detect": "^2.1.2",
    "react-dom": "17.0.2",
    "react-intersection-observer": "^8.33.1",
    "react-player": "^2.9.0"
  },
  "devDependencies": {
    "@emotion/babel-plugin": "^11.7.2",
    "next": "12.0.9",
    "express": "^4.17.2"
  }
}
