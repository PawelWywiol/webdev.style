---
const { theme, class: className } = Astro.props;
const validTheme = theme === 'dark' ? 'dark' : undefined;
---

<section class={[validTheme, className].filter(Boolean).join(' ')}>
  <slot name="nav" />
  <article>
    <slot />
  </article>
</section>
